#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int m,n,i,j,c;
    scanf("%d%d",&m,&n);
    for(i=m;i<=n;i++)
    {
       c=0; 
        for(j=1;j<=i;j++)
        {
            if(i%j==0)
            c++;
            }
        if(c==2)
        {
        printf("%d ",i);
        }
    }
   
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
